PPI_BACKEND_HOST=127.0.0.1
PPI_BACKEND_PORT=8003
PPI_TRIGGER_TOKEN=REPLACE_WITH_MAKEUP_TOKEN
PPI_PLUGIN_CATEGORY=makeup

# Keep these the same unless makeup uses a different table/reference setup.
PPI_TABLE_NAME=PPI CALCULATOR
RSP_TABLE_NAME=RSP REFERENCE-CPD
RSP_COLUMN_NAME=RSP(Y26 38)
PPI_LINK_FIELD=Link
