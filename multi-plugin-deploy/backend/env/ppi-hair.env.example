PPI_BACKEND_HOST=127.0.0.1
PPI_BACKEND_PORT=8002
PPI_TRIGGER_TOKEN=REPLACE_WITH_HAIR_TOKEN
PPI_PLUGIN_CATEGORY=hair

# Keep these the same unless hair uses a different table/reference setup.
PPI_TABLE_NAME=PPI CALCULATOR
RSP_TABLE_NAME=RSP REFERENCE-CPD
RSP_COLUMN_NAME=RSP(Y26 38)
PPI_LINK_FIELD=Link
